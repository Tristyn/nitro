import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.solananitro.app",
  appName: "Solana Nitro",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
